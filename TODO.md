- [x] Inspect current Supabase migrations for `public.attendance` table
- [x] Create new Supabase migration to add `check_out timestamptz` column to `public.attendance`
- [x] Ensure migration includes `IF NOT EXISTS` and sensible RLS/no-policy changes (if any)
- [ ] Run typecheck / build (if available) and optionally verify migration SQL formatting


